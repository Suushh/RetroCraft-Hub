// Add this JavaScript in your script file or in a <script> tag in your HTML file
function performSearch() {
    // Get the value entered in the search input
    var searchTerm = document.getElementById("searchInput").value;

    // Perform your search logic here (e.g., redirect to a search results page, filter content, etc.)
    alert("Searching for: " + searchTerm);
}
